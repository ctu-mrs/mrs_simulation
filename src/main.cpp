#include <ros/ros.h>

/*
 * NOTE: this is a dummy code
 * The implementation is written in Python, folder ../scripts
 */

int main(int argc, char** argv) {

  ros::init(argc, argv, "mrs_simulation");
  ROS_INFO("Success!");
  return 0;
}
